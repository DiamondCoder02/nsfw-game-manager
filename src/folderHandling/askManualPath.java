package folderHandling;

public class askManualPath {
	public static boolean askManual() {
		// TODO - check if mainProgramStart.java has both steamDir and mainDir set
		// if not ask the user to manually set the path

		return false;
	}
}
